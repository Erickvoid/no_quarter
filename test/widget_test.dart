import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — Hive needs initialization before running full app tests.
    expect(true, isTrue);
  });
}
